package web

import (
	"fmt"
	"unshift.local/paybaq/models"
)

templ PageLedger(ledger models.Ledger) {
	<div class="sm:p-4 md:p-8 " data-init={ fmt.Sprintf("@get('/ledgers/%s/stream')", ledger.Id) }>
		@Ledger(ledger)
	</div>
}
